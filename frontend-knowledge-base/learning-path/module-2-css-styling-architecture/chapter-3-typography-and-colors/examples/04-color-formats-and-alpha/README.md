# Example 4: Color Formats and Alpha Transparency

This example demonstrates:
- named colors
- hex colors
- `rgb()` and `rgba()`
- `hsl()` and `hsla()`
- using transparency for shadows and overlays

Open `index.html` in a browser and compare the cards to see how each CSS color format represents similar visual ideas.