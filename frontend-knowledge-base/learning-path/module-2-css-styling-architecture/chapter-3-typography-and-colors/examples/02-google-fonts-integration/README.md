# Example 2: Google Fonts Integration

This example demonstrates:
- loading a Google Font with `<link>`
- applying different font weights
- combining a web font with a fallback stack
- keeping text visible with `display=swap`

Open `index.html` in a browser with internet access and compare the heading and body text once the font loads.