# Example 1: Font Stacks and Type Scale

This example demonstrates:
- safe font stacks with fallbacks
- heading and body type hierarchy
- `rem`-based sizing
- readable `line-height`

Open `index.html` in a browser and compare the heading, subtitle, body copy, and code sample. Inspect the styles to see how the type scale is built from the root font size.