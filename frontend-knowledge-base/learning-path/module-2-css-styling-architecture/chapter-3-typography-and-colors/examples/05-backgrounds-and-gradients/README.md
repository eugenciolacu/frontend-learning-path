# Example 5: Backgrounds and Gradients

This example demonstrates:
- `background-color`
- layered `background-image`
- `background-repeat`
- `background-position`
- `background-size`
- `linear-gradient()` overlays

Open `index.html` in a browser and inspect how the hero card combines a repeated grid pattern, a radial highlight, and a linear gradient.