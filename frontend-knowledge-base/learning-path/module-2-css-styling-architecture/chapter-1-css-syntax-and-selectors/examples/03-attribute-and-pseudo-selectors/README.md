# Example 3: Attribute and Pseudo Selectors

This example demonstrates:
- attribute selectors
- `:hover`
- `:focus`
- `:nth-child()`
- `::before`
- `::selection`

Use both mouse and keyboard when testing this example so you can see hover and focus differences.