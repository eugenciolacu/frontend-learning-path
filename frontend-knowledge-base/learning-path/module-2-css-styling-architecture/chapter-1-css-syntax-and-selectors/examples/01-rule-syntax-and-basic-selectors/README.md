# Example 1: Rule Syntax and Basic Selectors

This example demonstrates:
- external CSS
- rule anatomy
- element selectors
- class selectors
- id selectors
- grouping selectors

Open `index.html` in a browser and inspect how each selector targets a different part of the page.