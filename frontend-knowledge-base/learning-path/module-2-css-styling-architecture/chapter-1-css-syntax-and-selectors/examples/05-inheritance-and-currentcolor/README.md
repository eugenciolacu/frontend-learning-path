# Example 5: Inheritance and `currentColor`

This example shows which text-related properties inherit from a parent container and how links can reuse the current text color.

Open the page and compare the article text, the link, and the badge border.