# Example 1: Viewport and Mobile-First Layout

This example demonstrates:
- the required viewport meta tag
- a mobile-first card layout
- a larger-screen enhancement with `min-width`
- flexible spacing and container sizing

Open `index.html` in a browser and resize the viewport. Start from a narrow width and observe how the stacked layout becomes a two-column layout when there is enough space.