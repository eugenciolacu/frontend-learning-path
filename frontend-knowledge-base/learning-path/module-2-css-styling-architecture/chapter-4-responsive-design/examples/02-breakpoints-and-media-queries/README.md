# Example 2: Breakpoints and Media Queries

This example demonstrates:
- content-based breakpoints
- `min-width` media queries
- a layout that grows from one column to two and then three columns
- how a component changes spacing and alignment at each stage

Resize the viewport slowly and watch when the card grid changes. The breakpoint values are chosen because the content starts to feel cramped, not because of a device label.