# Example 5: Fluid Typography with Clamp

This example demonstrates:
- responsive typography using `clamp()`
- fluid spacing values
- a readable article width using `ch`
- how text can scale smoothly without multiple font-size breakpoints

Open the page and resize the viewport gradually. Notice that the heading and supporting text scale smoothly rather than jumping at a single breakpoint.