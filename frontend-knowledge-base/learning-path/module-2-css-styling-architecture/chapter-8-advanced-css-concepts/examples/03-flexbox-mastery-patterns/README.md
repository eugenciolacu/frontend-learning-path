# Example 3: Flexbox Mastery Patterns

This example demonstrates:
- main-axis and cross-axis alignment
- a toolbar that uses auto margins correctly
- wrapping cards with flexible basis values
- `min-width: 0` to prevent overflow surprises in flex items

Open `index.html`, resize the viewport, and watch how the chips and cards wrap while keeping readable spacing.
