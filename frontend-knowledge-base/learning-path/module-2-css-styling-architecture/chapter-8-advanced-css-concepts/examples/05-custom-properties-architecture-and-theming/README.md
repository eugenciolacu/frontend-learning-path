# Example 5: Custom Properties Architecture and Theming

This example demonstrates:
- raw tokens, semantic tokens, and component tokens
- light and dark theme scopes driven by data attributes
- local component overrides without changing markup structure
- how a token architecture supports maintainable theming

Open `index.html` and use the theme switcher. Notice that the component rules stay mostly unchanged while the token values change.
