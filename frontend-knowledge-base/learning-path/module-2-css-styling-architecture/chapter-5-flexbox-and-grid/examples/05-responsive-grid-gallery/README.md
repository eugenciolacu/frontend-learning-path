# Example 5: Responsive Grid Gallery

This example demonstrates:
- `repeat(auto-fit, minmax(...))` for responsive columns
- how Grid can adapt without several explicit breakpoints
- a gallery-style layout with consistent spacing
- a practical comparison to fixed-width card rows

Open `index.html` and resize the viewport gradually. Notice how the browser automatically changes the number of columns based on the available space.
