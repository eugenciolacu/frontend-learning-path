# Example 3: Grid Columns and Gap

This example demonstrates:
- a basic multi-column grid
- the `fr` unit for proportional sizing
- spacing with `gap`
- card alignment inside a structured two-dimensional layout

Open `index.html` and resize the page. The grid will switch from one column to a three-column layout at a wider breakpoint so you can compare how Grid controls columns explicitly.
