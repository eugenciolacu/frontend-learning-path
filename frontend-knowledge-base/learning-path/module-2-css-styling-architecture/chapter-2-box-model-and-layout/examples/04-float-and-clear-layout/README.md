# Example 4: Float and Clear Layout

This example demonstrates:
- `float: left`
- text wrapping around a floated element
- `clear: both`
- a common legacy layout behavior

Open `index.html` in a browser and observe how text wraps around the floated image card until the cleared footer starts below it.