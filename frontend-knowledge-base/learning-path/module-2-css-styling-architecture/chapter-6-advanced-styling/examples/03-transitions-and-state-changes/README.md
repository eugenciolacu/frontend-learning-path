# Example 3: Transitions and State Changes

This example demonstrates:
- transition shorthand for multiple properties
- smooth hover and focus state changes on cards and buttons
- a reveal panel that changes opacity and position
- how subtle timing improves perceived responsiveness

Open `index.html` in a browser. Hover the cards and button, and compare the static layout with the smoother transition-based version.