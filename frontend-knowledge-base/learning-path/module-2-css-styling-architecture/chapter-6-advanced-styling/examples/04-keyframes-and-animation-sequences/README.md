# Example 4: Keyframes and Animation Sequences

This example demonstrates:
- custom `@keyframes` animations
- a loading indicator animated with transforms
- a notification entrance animation using opacity and translateY
- reduced-motion support with `prefers-reduced-motion`

Open `index.html` in a browser and observe the looping loader and the single entrance animation. This example focuses on small, purposeful motion instead of decorative overload.