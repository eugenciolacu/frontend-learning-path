# Example 5: CSS Variables and Theming

This example demonstrates:
- global custom properties on `:root`
- local variable overrides inside a component
- theme variations driven by variable reassignment
- reuse of variables for color, spacing, radius, and shadow values

Open `index.html` in a browser and compare the default and contrast cards. The component structure stays the same while the theme values change.