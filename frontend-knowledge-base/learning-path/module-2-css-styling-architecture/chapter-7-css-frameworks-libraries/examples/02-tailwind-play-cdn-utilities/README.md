# Example 2: Tailwind Play CDN Utilities

This example demonstrates:
- loading Tailwind with the Play CDN for learning and quick prototypes
- composing a layout from utility classes only
- responsive grid and flex classes
- utility-first spacing, typography, color, and shadow patterns

Open `index.html` in a browser. This example does not need a separate CSS file because the styling lives in the HTML class list.
