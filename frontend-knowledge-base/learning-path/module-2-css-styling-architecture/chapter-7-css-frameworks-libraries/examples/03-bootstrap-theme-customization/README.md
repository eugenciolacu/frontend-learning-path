# Example 3: Bootstrap Theme Customization

This example demonstrates:
- using Bootstrap from a CDN
- overriding Bootstrap CSS variables to create a custom theme
- keeping Bootstrap's component structure while changing color, radius, and shadow values
- adding a small amount of project-specific CSS without fighting the framework

Open `index.html` in a browser. Compare the framework structure with the custom visual identity created in `styles.css`.
