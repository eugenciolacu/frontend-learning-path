# Example 1: Bootstrap CDN Layout

This example demonstrates:
- adding Bootstrap from a CDN
- using the Bootstrap grid for a responsive card layout
- combining component classes such as `card` and `btn`
- using utility classes for spacing, alignment, and typography

Open `index.html` in a browser. Resize the page to see the grid move from one column to two and then three columns.
